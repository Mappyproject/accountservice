server:
  port: 8084

spring:
  application:
    name: ACCOUNT-SERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone : http://mappy_eurekaserver:8761/eureka/
    register-with-eureka: true
    fetch-registry: true