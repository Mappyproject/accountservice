server:
  port: 8084

spring:
  application:
    name: ACCOUNT-SERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone : http://mappy-eureka:8761/eureka/
    register-with-eureka: true
    fetch-registry: true